# 骰子诗句

六个随机词，拼凑你的诗 —— 移动端 H5 互动小诗生成器。

## 本地运行

```bash
./start.sh
```

浏览器打开 http://localhost:3850

## 构建

```bash
./build.sh
```

将 `js/*.js` 打包为 `js/app.bundle.js`。

## 技术栈

纯 HTML / CSS / JavaScript，无框架依赖，可部署至 GitHub Pages 或任意静态托管。
